namespace SlimeLab.Systems
{
    public enum ExpeditionStatus
    {
        Preparing,  // Expedition is being prepared (adding slimes)
        Active,     // Expedition is currently active
        Completed,  // Expedition completed successfully
        Failed      // Expedition failed
    }
}
